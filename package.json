{
  "name": "orbital-estates",
  "version": "0.1.0",
  "private": true,
  "description": "Realtime circular-board property trading game with an Expo React Native client and scalable TypeScript server.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:server": "npm run dev -w @orbital-estates/server",
    "dev:mobile": "npm run start -w @orbital-estates/mobile",
    "build": "npm run build -w @orbital-estates/shared && npm run build -w @orbital-estates/server",
    "test": "npm run test -w @orbital-estates/shared",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest",
    "vitest": "latest"
  }
}
